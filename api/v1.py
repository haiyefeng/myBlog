# -*- coding: utf-8 -*-

from django.conf.urls import include, url


urlpatterns = [
    # 健康维度模块
    # url(r'^health_dimension/', include('app_portrait.health_dimension.urls')),
    # url(r'^application/', include('app_portrait.application.urls')),
]
